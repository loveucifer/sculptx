import 'package:flutter/material.dart';
import 'app.dart';

/// The main entry point for the SculptX application.
void main() {
  // runApp() inflates the given widget and attaches it to the screen.
  runApp(const SculptXApp());
}
